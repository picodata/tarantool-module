apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tarantool.fullname" . }}-test-connection"
  labels:
{{ include "tarantool.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tarantool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
